export default function Day016() {
  return (
    <iframe
      src={`${import.meta.env.BASE_URL}works/day16/index.html`}
      style={{ width: '100%', height: '100%', border: 'none' }}
      title="Day 16: Wait, is it summer already?"
    />
  )
}
